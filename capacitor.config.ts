import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tr.com.khg',
  appName: 'ionic-course-1-section',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
